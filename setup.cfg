[tool:pytest]
asyncio_mode = auto
testpaths = tests

[coverage:run]
source = custom_components/trading212
omit = custom_components/trading212/translations/*

[coverage:report]
exclude_lines =
    pragma: no cover
    if TYPE_CHECKING:
